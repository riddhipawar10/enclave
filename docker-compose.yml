version: '3.8'

services:
  postgres:
    image: postgres:16
    container_name: enclave-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: enclave_db
      POSTGRES_USER: enclave_user
      POSTGRES_PASSWORD: enclave_pass
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7
    container_name: enclave-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  postgres_data:
  redis_data:
